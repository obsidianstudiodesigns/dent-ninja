# OS
Thumbs.db
desktop.ini
.DS_Store

# editors
.vscode/
.idea/
*.swp

# node (not used today, but keeps the repo clean if tooling is added later)
node_modules/

# client media masters (60 MB) - processed copies live in assets/
Work done/
